---
import Layout from "../layouts/Layout.astro";
import DataTable from "@/components/FinesTable/DataTable";
import type { TotalFines } from "@/shared/types";
import { getPlayerFines } from "@/utils/data-helper";

const playerFines: TotalFines[] = getPlayerFines();
---

<Layout title="footy fines">
  <DataTable data={playerFines} client:load />
</Layout>
